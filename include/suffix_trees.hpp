#pragma once

#include "definiciones.hpp"

class SuffixTrees {
    public:
        static void buscar(const std::string& texto, const std::string& patron, std::vector<int>& posiciones);

    private:
        // Agregar todas las funciones privadas necesarias para implementar el algoritmo de Suffix Trees
};